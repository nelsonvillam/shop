apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# EKS deployment (used by CI/CD pipeline).
# For local Docker Desktop deployment use: kubectl apply -k k8s/overlays/local/
resources:
  - overlays/eks
