apiVersion: v1
kind: Secret
metadata:
  name: mongodb-keyfile
  namespace: shop
type: Opaque
stringData:
  # All 3 replica set members must share this exact key.
  # Generate with: openssl rand -base64 756
  keyfile: CHANGE_ME
