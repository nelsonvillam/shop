<?xml version="1.0"?>
<ruleset name="Custom Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>PMD ruleset for shop project</description>

    <rule ref="category/java/bestpractices.xml">
        <exclude name="GuardLogStatement"/>
        <exclude name="JUnitTestContainsTooManyAsserts"/>
        <exclude name="JUnitAssertionsShouldIncludeMessage"/>
        <!-- MongoDB Document is the correct type in MongoDB config classes -->
        <exclude name="LooseCoupling"/>
    </rule>

    <rule ref="category/java/errorprone.xml">
        <exclude name="NullAssignment"/>
        <!-- Annotation parameters like "404", "bsonType" are intentional string literals -->
        <exclude name="AvoidDuplicateLiterals"/>
        <!-- Simple numeric guards like stock < 1 are readable and intentional -->
        <exclude name="AvoidLiteralsInIfCondition"/>
    </rule>

</ruleset>
